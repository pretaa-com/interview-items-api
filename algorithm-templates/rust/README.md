# Rust — same structure

Implement `same_structure_as(this_array: &[Value], other: &Value) -> bool` in `src/lib.rs`. The `Value` enum represents nested structure: `Value::Array(Vec<Value>)` for arrays and `Value::Primitive` for non-array elements. Make all tests pass.

## Run tests

```bash
cargo test
```
