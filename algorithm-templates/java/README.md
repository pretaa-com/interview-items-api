# Java — same structure

Implement `SameStructureAs.sameStructureAs(List<?> thisArray, Object other)` in `src/main/java/algorithm/SameStructureAs.java` so all tests pass.

## Run tests

```bash
./gradlew test
```
