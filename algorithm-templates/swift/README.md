# Swift — same structure

Implement `sameStructureAs(_ thisArray: [Any], other: Any) -> Bool` in `Sources/SameStructureAs/SameStructureAs.swift` so all tests pass.

## Run tests

```bash
swift test
```
