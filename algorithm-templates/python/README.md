# Python — same structure

Implement `same_structure_as(this_array, other)` in `same_structure_as.py` so all tests pass.

## Run tests

From this directory:

```bash
pip install pytest
pytest
```

Or with verbose output: `pytest -v`
