namespace Algorithm;

/// <summary>
/// Return true if and only if other is an array/list and has the same nesting
/// structure and same lengths of nested arrays as thisArray.
/// </summary>
public static class SameStructureAs
{
    public static bool Run(object[] thisArray, object? other)
    {
        // TODO: implement
        return false;
    }
}
